# Deepen the docs Concepts section — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the 8 one-paragraph Concept stubs in `app/docs/page.tsx` with 13 source-grounded, developer-oriented articles in two TOC groups, correct the live factual errors, and make a narrow precision pass on `app/technology/page.tsx`.

**Architecture:** All Concept content lives in one file, `app/docs/page.tsx`: a TOC `<nav data-docs-nav>` of `<h2>` groups + `<li><a href="#id">` links, and matching `<article id className={styles.prose}>` blocks inside `<div data-docs-main>`. The `DocsScroll` scroll-spy couples nav links to article ids — every article id needs a matching nav `<li>` and vice-versa. Tasks run **sequentially** (never parallel) since they share this file.

**Tech Stack:** Next.js (App Router), React, TypeScript, existing docs UI (`CodeBlock`, `Table`, raw `note`/`doc-card`, `<h2>/<h3>`, `<ul>`).

## Global Constraints

- **No AI/authorship metadata in any content.** The docs must read as first-party
  Lineage documentation: NO "generated by/with AI", no model names, no
  Co-Authored-By, no meta commentary about how the content was produced, no
  "as an AI" phrasing. This applies to all prose, comments, and code blocks added
  to the site. (Commit-message trailers are separate — see note at end.)
- **Source of truth = the design spec** `docs/superpowers/specs/2026-09-17-docs-concepts-deep-design.md`. It carries the verified facts, file citations, and example JSON/code for every article. Implementers MUST read their article's spec section and **verify each cited fact against the named source file** (in the sibling `prime`/`fleet`/`valence` repos, `public/openapi.json`, or `sdk-js`) before stating it. State nothing not backed by the spec/source; where a fact is unimplemented or version-specific, say so honestly.
- Verification per task (no unit-test harness): `npm run build -- --webpack` succeeds (plain `npm run build` fails on this darwin/arm64 sandbox — always `-- --webpack`) AND `npm run lint` is clean (one pre-existing unrelated `design-prototype` warning is acceptable). Plus: every article id has a matching nav `<li>` and vice-versa (grep), and `/docs#c-unicorn` + `/docs#c-block-mining` anchors still resolve.
- Keep the `#c-block-mining` and `#c-unicorn` article ids alive (external links point at them). Reuse existing ids where the spec keeps them.
- Distinguish asset wire-form `{ "Token": n }` / `{ "Item": {…} }` from REST `ApiAsset` `{ "kind": "token", … }` in examples — never mix.
- No new raw hex in stylesheets. Reuse existing components; do not add new deps.
- Commit after each task. Commit-message trailer for each commit:
  ```
  Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01GiTkeAEoDShsufZCLx4EwV
  ```

## TOC target (end state, for reference)

By the end, the Concepts `<nav>` region has two `<h2>` groups replacing the single
`<h2>Concepts</h2>`:

```
<h2>Concepts · Data &amp; transactions</h2>
<ul>
  <li><a href="#c-data-model">Data model</a></li>
  <li><a href="#c-keys">Keys, addresses &amp; wallets</a></li>
  <li><a href="#c-transactions">Transactions</a></li>
  <li><a href="#c-scripts">Scripts</a></li>
  <li><a href="#c-two-way">Two-way (DRUID) payments</a></li>
  <li><a href="#c-valence">The valence relay</a></li>
</ul>
<h2>Concepts · Network &amp; consensus</h2>
<ul>
  <li><a href="#c-node-types">Node types</a></li>
  <li><a href="#c-mempool">Mempool node</a></li>
  <li><a href="#c-storage">Storage node</a></li>
  <li><a href="#c-miner">Miner node</a></li>
  <li><a href="#c-block-mining">Consensus &amp; the block round</a></li>
  <li><a href="#c-blocks">Blocks &amp; headers</a></li>
  <li><a href="#c-unicorn">UNiCORN randomness</a></li>
</ul>
```

Each task adds only the `<li>`s for the articles it creates/keeps, so after every
task the TOC lists exactly the articles that exist.

---

### Task 1: TOC groups + Data model, Keys, Transactions (deepen), Scripts

**Files:** Modify `app/docs/page.tsx`

Read spec sections **1 (Data model), 2 (Keys/addresses/wallets), 3 (Transactions), 4 (Scripts)** and verify facts against `prime` (`asset.rs`, `transaction.rs`, `crypto.rs`, `transaction_utils.rs`, `script/mod.rs`, `script/lang.rs`, `constants.rs`) and the balance/tx JSON against `public/openapi.json` + `sdk-js`.

- [ ] **Step 1: Split the Concepts TOC into two group headings**

In the `<nav data-docs-nav>` block, replace the single `<h2>Concepts</h2>` + its `<ul>` (the 8 existing `<li>`s) with the **two** `<h2>` group headings from the TOC target. Under "Data &amp; transactions" include the `<li>`s for `#c-data-model`, `#c-keys`, `#c-transactions`, `#c-scripts` now (this task) and leave `#c-two-way`/`#c-valence` for Task 2. Under "Network &amp; consensus" keep the existing `#c-node-types`, `#c-mempool`, `#c-storage`, `#c-miner`, `#c-block-mining` (relabel its text to "Consensus &amp; the block round"), `#c-unicorn` `<li>`s (deepened in later tasks), and add `#c-blocks` in Task 4. Net after Task 1: every `<li>` maps to an existing article (the new ones created below; the network ones are the current stubs).

- [ ] **Step 2: Add the "Data model" article** (`#c-data-model`)

Insert a new `<article id="c-data-model" className={styles.prose}>` at the start of the Concepts articles (before `#c-node-types`). Content per spec §1: the UTXO ledger; `Asset = Token(u64) | Item{amount, genesis_hash?, metadata?}`; the native token (brand **LNGX**; note it's a network/brand name, the integer amount is what the code carries) with base-unit divisor 72,072,000 and the supply cap, linking `/tokenomics`; items via `genesis_hash` + metadata (≤800 bytes). Include the balance JSON example (spec §1) in a `<CodeBlock lang="json">`. Cross-link `/developers/api` and `#c-transactions`.

- [ ] **Step 3: Add the "Keys, addresses & wallets" article** (`#c-keys`)

New `<article id="c-keys">` after data-model. Content per spec §2: ed25519 keys; `address = hex(sha3_256(public_key))` (64 hex chars; note legacy schemes exist); BIP39/BIP32 wallets; **signing = SHA3-256 over `json(each TxOut) ++ json(previous_out)`** (outputs + prev outpoint only; excludes fees/druid_info/own scriptSig) — hence "sign exactly what you submit; field order is load-bearing." Optional short sdk-js keypair snippet (`initNew` → `getNewKeypair` → `address`). Cross-link `#c-transactions`, `#c-scripts`.

- [ ] **Step 4: Deepen the "Transactions" article** (`#c-transactions`)

Replace the one-paragraph body of the existing `<article id="c-transactions">` with the deepened content per spec §3: the `Transaction { inputs, outputs, version, fees, druid_info }` struct (field order load-bearing; txid = sha3(bincode)+'g'+32); `TxIn`/`OutPoint`/`TxOut`; the `version` nuance (stamped, not branched on — do NOT say "version 2 == two-way"); `fees` as balanced outputs with no fixed fee policy yet. Include the `POST /v1/transactions` example JSON (spec §3) in a `<CodeBlock lang="json">` and a short sdk-js `makeTokenPayment` snippet (`<CodeBlock lang="javascript">`). Note the wire-form vs `ApiAsset` distinction. Move this article into the "Data & transactions" position (after `#c-keys`).

- [ ] **Step 5: Add the "Scripts" article** (`#c-scripts`)

New `<article id="c-scripts">` after transactions. Content per spec §4: bounded, loop-free (Turing-incomplete) stack machine; the opcode families (compact list, incl. `OP_CREATE` for items and `OP_CHECKSIG`), the limits; P2PKH lock/unlock (the `pay2pkh` push sequence) and how a spend validates; multisig/P2SH exist. Optional small pseudo-script `<CodeBlock>`.

- [ ] **Step 6: Build, lint, verify**

Run `npm run build -- --webpack && npm run lint` → clean. Then:
`grep -oE 'id="c-[a-z-]+"' app/docs/page.tsx` and `grep -oE 'href="#c-[a-z-]+"' app/docs/page.tsx` — every id created/kept so far has a matching nav link and vice-versa (no orphans among the articles that exist). Confirm no AI/meta phrasing added: `grep -niE "generated (by|with)|as an ai|co-authored|language model|gpt|claude" app/docs/page.tsx` → no hits in the new content.

- [ ] **Step 7: Commit** (`docs(concepts): add data model, keys, scripts; deepen transactions`)

---

### Task 2: Two-way (DRUID) deepen + valence relay

**Files:** Modify `app/docs/page.tsx`

Read spec sections **5 (Two-way/DRUID)** and **6 (valence)**; verify against `prime` (`druid.rs`, `druid_utils.rs`), `sdk-js` (`make2WayPayment`/`accept2WayPayment`), and `valence` (`README`, `auth.rs`, `messages.rs`, `store.rs`).

- [ ] **Step 1: Deepen the "Two-way (DRUID) payments" article** (`#c-two-way`)

Replace the existing 3-sentence body per spec §5: atomic swap (both halves settle or neither); signalled by `druid_info: Some(DdeValues{druid, participants, expectations:[{from,to,asset}], genesis_hash})`, which is **unsigned**; matched structurally by expectation-checking against outputs (not by a signature or a version number). The sdk-js flow (`make2WayPayment` → `fetchPending2WayPayment` → `accept2WayPayment`) with a short snippet; note offers ride on valence and the reference SDK currently posts them unencrypted. Retitle nav label to "Two-way (DRUID) payments".

- [ ] **Step 2: Add the "valence relay" article** (`#c-valence`)

New `<article id="c-valence">` after two-way. Content per spec §6: an opaque, E2E-encrypted, per-mailbox Redis relay (carries offers but doesn't model them); mailbox = address, entries keyed by a caller `id` (DRUID is just an example id); TTL ~600s. Auth headers `address`/`public_key`/`signature = ed25519(sk, utf8(address))`, **verify-only** (no address↔pubkey binding, by design). The `/messages` routes (POST/GET/DELETE) + `GET /healthz`. **Honesty note**: the pending→accepted lifecycle lives in the SDK, not valence.

- [ ] **Step 3: Add the two nav `<li>`s** for `#c-two-way` (already exists — just relabel) and `#c-valence` (new) under the "Data & transactions" group, in order after `#c-scripts`.

- [ ] **Step 4: Build, lint, verify** (as Task 1 Step 6: build+lint clean; id↔nav parity; no AI/meta phrasing grep).

- [ ] **Step 5: Commit** (`docs(concepts): deepen two-way payments; add the valence relay`)

---

### Task 3: Node types (correct 3→5) + Mempool / Storage / Miner (deepen)

**Files:** Modify `app/docs/page.tsx`

Read spec section **7 (Node types)** and **8/9/10 (mempool/storage/miner)**; verify against `fleet` (`interfaces.rs:259` NodeType, the `bins/*`, `mempool_raft.rs`, `storage_raft.rs`, `fleet-storage/src/lib.rs`, `fleet-miner`, `utils.rs` reward/coinbase).

- [ ] **Step 1: Correct + deepen "Node types"** (`#c-node-types`)

Replace the body to cover the **five** roles (`NodeType { Miner, Storage, Mempool, User, PreLaunch }`): mempool, storage, miner, user (all serve `/v1`), and **pre_launch** (one-shot bootstrap, NO HTTP API). Present as a small `<Table>` (role · job · serves `/v1`?). Cross-link `/technology#subsystems`.

- [ ] **Step 2: Deepen Mempool / Storage / Miner** (`#c-mempool`, `#c-storage`, `#c-miner`)

Replace each body per spec §8/9/10. Mempool: tx intake → RAFT replication → runs the round → assembles winner → sends to storage. Storage: validates PoW, RAFT-replicates parts, reassembles + persists + indexes, seeds next round — **and does NOT currently re-verify the UNiCORN VDF** (remove any such implication). Miner: coinbase + SHA3-256 PoW over the merkle root; a UNiCORN-selected subset + winner; decaying reward (no halving), split across the mempool quorum, 100-block coinbase maturity. Each links `#c-block-mining` for round detail.

- [ ] **Step 3: Build, lint, verify** (build+lint clean; id↔nav parity; **grep `app/docs/page.tsx` for "three" near node copy and for any remaining "UNiCORN linkage"/"re-check" in storage** → corrected; no AI/meta phrasing).

- [ ] **Step 4: Commit** (`docs(concepts): correct node roles to five; deepen mempool/storage/miner`)

---

### Task 4: Consensus & the block round + Blocks & headers + UNiCORN rewrite

**Files:** Modify `app/docs/page.tsx`

Read spec sections **11 (Consensus & block round)**, **12 (Blocks & headers)**, **13 (UNiCORN)**; verify against `fleet` (`block_pipeline.rs`, `mempool_raft.rs`, `asert.rs`, `miner_pow/`, `unicorn.rs`), `prime` (`block.rs`), and whitepaper facts in the spec.

- [ ] **Step 1: Expand `#c-block-mining` into "Consensus & the block round"**

Keep the id `#c-block-mining` (external links depend on it); change the `<h2>` to "Consensus & the block round". Content per spec §11: **Prime Radiant Consensus** (whitepaper name; link `/technology#consensus` for the DPoWW/economic framing — do not invent DPoWW wording here); two RAFT groups + deterministic replicated state; the round lifecycle (intake → UNiCORN participant select → PoW → UNiCORN winner → assemble → storage commit → seed next); **ASERT** difficulty with the fixed-interval/variable-hashes mapping; fixed **30 s** blocks, height ≡ UTC, epoch 2026-08-28, 2880 blocks/day; **SHA3-256** PoW (GPU backends) — do not claim fleet mines with SandWorm today (link `/technology` for the SandWorm direction).

- [ ] **Step 2: Add "Blocks & headers"** (`#c-blocks`)

New `<article id="c-blocks">` after `#c-block-mining`. Content per spec §12: the `BlockHeader` fields (`version, bits, nonce_and_mining_tx_hash, b_num, timestamp, seed_value (UNiCORN "{seed}-{witness}"), previous_hash, txs_merkle_root_and_hash` = a MerkleLog root + flat SHA3 **pair**, not "nested"); `Block { header, transactions: Vec<String> }`. Reading via `GET /v1/blocks/latest` / `{num}` (link `/developers/api`; note `/latest`'s `block` is opaque — don't invent fields beyond the struct). Add its nav `<li>` under "Network & consensus".

- [ ] **Step 3: Rewrite "UNiCORN randomness"** (`#c-unicorn`)

Replace the body per spec §13: a **Sloth VDF** (slow-eval/fast-verify) producing publicly verifiable, hard-to-bias ("uncontestable") randomness; seed = sha3 over (round tx inputs, participating-miner list, winning hashes from two blocks ago); recomputed identically on every node from RAFT-replicated inputs; used via a Fortuna CSPRNG to select the mining subset and the winner; stamped into `header.seed_value`; whitepaper's daily mempool-triple rotation. **Do NOT** say storage re-checks it.

- [ ] **Step 4: Build, lint, verify** — build+lint clean; **full** id↔nav parity now (every article id in the file has exactly one nav `<li>` and vice-versa; run both greps and diff the id lists); `/docs#c-unicorn` and `/docs#c-block-mining` ids present; no AI/meta phrasing grep.

- [ ] **Step 5: Commit** (`docs(concepts): consensus & block round, blocks & headers; rewrite UNiCORN`)

---

### Task 5: Narrow `/technology` reconciliation

**Files:** Modify `app/technology/page.tsx`

Read spec section **"Narrow /technology reconciliation"**. Keep SandWorm and DPoWW (real, separately sourced). Only:

- [ ] **Step 1: Consensus naming** — in the `#consensus` section, lead with **"Prime Radiant Consensus"** as the name; keep the DPoWW paper link but present DPoWW as the underlying scheme rather than the headline. Light copy edit only; do not remove SandWorm or the DPoWW link.

- [ ] **Step 2: Merkle wording** — in the `#subsystems` table, change "nested Merkle root" to accurate wording (e.g. "a Merkle root over the block's transactions"). Remove "nested".

- [ ] **Step 3: op-ai-evaluate** — in the `#moat` card mentioning the `op-ai-evaluate` opcode, reword so it reads as forward-looking/planned (e.g. "a planned `op-ai-evaluate` opcode") rather than shipped, since it is not in the current opcode set.

- [ ] **Step 4: Build, lint, verify** — build+lint clean; `/technology` renders; its `/docs#c-unicorn` link still present; no AI/meta phrasing added.

- [ ] **Step 5: Commit** (`docs(technology): align consensus naming; fix merkle wording; mark op-ai-evaluate planned`)

---

## Final Verification
- [ ] `npm run build -- --webpack` and `npm run lint` clean on the final tree.
- [ ] Live dev-server (`npx next dev --webpack`): the Concepts TOC shows both groups; every entry scroll-spies to its article; code blocks render; `/docs#c-unicorn` and `/docs#c-block-mining` scroll correctly; `/technology` renders and its `/docs#c-unicorn` link works.
- [ ] `grep -niE "generated (by|with)|as an ai|co-authored|language model|\bgpt\b|\bclaude\b" app/docs/page.tsx app/technology/page.tsx` → **no hits in shipped content** (no AI/authorship metadata).
- [ ] Spot-check: node types = 5; UNiCORN = Sloth VDF with no "storage re-checks"; two-way framed via `druid_info`; consensus called "Prime Radiant Consensus"; PoW = SHA3-256.
- [ ] `git diff main --stat` shows only `app/docs/page.tsx`, `app/technology/page.tsx`, and the spec/plan docs.

## Note on commit trailers vs the "no AI metadata" rule
The "no AI metadata in content" constraint governs the **site content**. This repo's
commit/PR convention (this session) still appends the Co-Authored-By / session
trailer to commit messages, which are dev artifacts, not shipped content. If you
want commits/PRs kept free of those signals too (as the `skills` repo does),
say so and they'll be omitted.
